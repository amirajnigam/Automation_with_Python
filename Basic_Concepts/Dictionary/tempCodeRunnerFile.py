message = 'it was a bright cold day in April, and the clocks were striking thirteen'
# count = {} # 'r': 12

# for character in message.upper(): #convert it to upper case
#     count.setdefault(character, 0) #right now the dict is empty we want to set char as Zero
#     count[character] = count[character] + 1

# print(count)
